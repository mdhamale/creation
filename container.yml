---
- hosts: local
  remote_user: jenkins
  become: yes
  become_method: sudo
  connection: ssh
  gather_facts: yes
  tasks:
  - name: create container
    command: 'lxc launch images:centos/7 data01'
